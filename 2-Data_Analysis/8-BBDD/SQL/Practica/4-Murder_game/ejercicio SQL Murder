-- You vaguely remember that the crime was a murder that occurred sometime on Jan.15, 2018 and that it took place in SQL City. 
-- Start by retrieving the corresponding crime scene report from the police department’s database.

SELECT date, type, description, city
FROM crime_scene_report
WHERE date = "20180115" AND type = "murder" AND city = "SQL City";
-----------------------------------------------------------------------

-- 1. The first witness lives at the last house on "Northwestern Dr". 
SELECT P.id, P.name, P.license_id, P.address_number, P.address_street_name, P.ssn, I.transcript
FROM person AS P
    INNER JOIN interview as I ON I.person_id==P.id
WHERE P.address_street_name = "Northwestern Dr"
ORDER BY P.address_number;
-- Morty Schapiro, address_number:4919
SELECT P.id, P.name, P.license_id, P.address_number, P.address_street_name, P.ssn, I.transcript
FROM person AS P
    INNER JOIN interview as I ON I.person_id==P.id
WHERE P.address_street_name = "Northwestern Dr" AND P.address_number = "4919";
--I heard a gunshot and then saw a man run out. He had a "Get Fit Now Gym" bag. The membership number on the bag started with "48Z". Only gold members have those bags. The man got into a car with a plate that included "H42W".

-- 2. The second witness, named Annabel, lives somewhere on "Franklin Ave".
SELECT P.id, P.name, P.license_id, P.address_number, P.address_street_name, P.ssn, I.transcript
FROM person AS P
    INNER JOIN interview as I ON I.person_id==P.id
WHERE P.address_street_name = "Franklin Ave"
ORDER BY P.name;
-- Annabel Miller, address_number: 103
SELECT P.id, P.name, P.license_id, P.address_number, P.address_street_name, P.ssn, I.transcript
FROM person AS P
    INNER JOIN interview as I ON I.person_id==P.id
WHERE P.name = "Annabel Miller" AND P.address_street_name = "Franklin Ave";
-- I saw the murder happen, and I recognized the killer from my gym when I was working out last week on January the 9th.
-----------------------------------------------------------------------

-- From 1.
SELECT id, person_id, name, membership_start_date, membership_status
FROM get_fit_now_member
WHERE id LIKE "48Z%" AND membership_status = "gold";
-- Joe Germuska (48Z7A), Jeremy Bowers (48Z55)
SELECT id, age, height, eye_color, hair_color, gender, plate_number, car_make, car_model
FROM drivers_license
WHERE plate_number LIKE "%H42W%";
-- Data about 3 cars (2 men, 1 woman)

-- From 2.
SELECT membership_id, check_in_date, check_in_time, check_out_time
FROM get_fit_now_check_in
WHERE membership_id = "48Z7A" OR membership_id = "48Z55";
-- Joe Germuska (48Z7A): 16:00/17:30, Jeremy Bowers (48Z55): 15:30/17:00
-----------------------------------------------------------------------

-- 3. Compruebo los nombres de los del gimnasio:
SELECT id, name, license_id, address_number, address_street_name, ssn
FROM person
WHERE name = "Joe Germuska" OR name = "Jeremy Bowers";
-- -- Joe Germuska (id: 28819; license_id: 173289), Jeremy Bowers (id: 67318; license_id: 423327)

-- 4. Compruebo las license_id con las de los 3 coches:
-- 423327 COINCIDE --> JEREMY BOWERS ES EL ASESINO
-----------------------------------------------------------------------

-- 5. Introduzco la solución
INSERT INTO solution VALUES (1, 'Jeremy Bowers'); 

SELECT value 
FROM solution;

-----------------------------------------------------------------------
-- Congrats, you found the murderer! But wait, there's more... If you think you're up for a challenge, try querying the interview transcript of the murderer to find the real villain behind this crime. 
-- If you feel especially confident in your SQL skills, try to complete this final step with no more than 2 queries. Use this same INSERT statement with your new suspect to check your answer.

-- 6. Mirar la entrevista a Jeremy Bowers
SELECT P.name, I.transcript
FROM person AS P
    INNER JOIN interview as I ON I.person_id==P.id
WHERE P.name = "Jeremy Bowers";
-- "I was hired by a woman with a lot of money. I don't know her name but I know she's around 5'5"" (65"") or 5'7"" (67""). She has red hair and she drives a Tesla Model S. I know that she attended the SQL Symphony Concert 3 times in December 2017.

-- 7. Sigo la pista
SELECT P.name, D.gender, D.height, D.hair_color, D.car_make, D.car_model, I.annual_income, F.event_name
FROM drivers_license AS D
    INNER JOIN person AS P ON P.license_id = D.id
    INNER JOIN income AS I ON I.ssn = P.ssn
    INNER JOIN facebook_event_checkin as F ON F.person_id = P.id
WHERE D.height BETWEEN "65" AND "67" AND D.gender = "female" AND D.hair_color = "red" AND D.car_make = "Tesla" AND D.car_model = "Model S" AND F.event_name = "SQL Symphony Concert";
-- Miranda Priestly

-- 8. Introduzco la solución
INSERT INTO solution VALUES (2, 'Miranda Priestly');

SELECT value 
FROM solution;